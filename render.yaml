services:
  - type: web
    name: the-floor
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: FLOOR_DEBATE_MS
        value: 90000
      - key: OPENAI_MODEL
        value: gpt-5.4-mini
